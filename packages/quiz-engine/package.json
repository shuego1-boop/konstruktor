{
  "name": "@konstruktor/quiz-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "type-check": "tsc --noEmit",
    "build": "tsc --project tsconfig.build.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@konstruktor/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.7",
    "@vitest/coverage-v8": "^4.0.7",
    "@vitest/ui": "^4.0.7"
  }
}
